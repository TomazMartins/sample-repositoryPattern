package br.com.tomazmartins.sampleRDP.external.infrastructure.db.specification;

/*
* SPECIFICATION:
*   A specification is a specific request to
*   the database, like when we want a list of
*   all entities of a determined type. Or, when
*   we want all entities that have a determined
*   attribute.
*
*   It will be handled in a SPECIFICATION.
*
*   In this way, we don't need worry with this until
*   the demand appear. And, plus, this new specification
*   will be reusable.
* */
public interface Specification {
    /*
     * It's only a regular marker interface.
     *   There are no need to do anything.
     */
}
